# Regressions

No regression against the remediated `df8f383` baseline was found. The changes made during this audit are covered by focused SDK/app unit tests and the repository build gates. Public SDK signature corrections are intentional pre-1.0 breaking corrections and are recorded in `CHANGELOG.md` and `docs/SDK_EXAMPLES.md`.
