# Documentation Drift

Fixed: `VeniceEndpoints` schema comment, API source manifest media status, SDK examples for chat safety/reasoning and queued media, parity matrix audio/video/image status, README capabilities, changelog breaking corrections, and source-baseline session evidence.

Remaining documentation risk is ordinary alpha drift: feature descriptions can outpace the three real screens. `docs/FEATURE_PARITY_MATRIX.md` remains the current status authority; presence in the drawer is never implementation evidence.
